# Tiny Tapeout project information
project:
  title:        "Handshake fsm"      # Project title
  author:       "G1"      # Your name
  discord:      ""      # Your discord username, for communication and automatically assigning you a Tapeout role (optional)
  description:  "explains about the handshake btw fsm "      # One line description of what your project does
  language:     "Verilog" # other examples include SystemVerilog, Amaranth, VHDL, etc
  clock_hz:     0       # Clock frequency in Hz (or 0 if not applicable)

  # How many tiles your design occupies? A single tile is about 167x108 uM.
  tiles: "1x1"          # Valid values: 1x1, 1x2, 2x2, 3x2, 4x2, 6x2 or 8x2

  # Your top module name must start with "tt_um_". Make it unique by including your github username:
  top_module:  "tt_um_example"

  # List your project's source files here.
  # Source files must be in ./src and you must list each source file separately, one per line.
  # Don't forget to also update `PROJECT_SOURCES` in test/Makefile.
  source_files:
    - "project.v"

# The pinout of your project. Leave unused pins blank. DO NOT delete or add any pins.
# This section is for the datasheet/website. Use descriptive names (e.g., RX, TX, MOSI, SCL, SEG_A, etc.).
pinout:
  # Inputs
  ui[0]: "req"        # Master request signal
  ui[1]: "rw"         # Read/Write: 1=READ, 0=WRITE
  ui[2]: ""           # Unused
  ui[3]: ""           # Unused
  ui[4]: ""           # Unused
  ui[5]: ""           # Unused
  ui[6]: ""           # Unused
  ui[7]: ""           # Unused

  # Outputs
  uo[0]: "ack"        # Acknowledge/grant
  uo[1]: "busy"       # Transaction in progress
  uo[2]: "done"       # One-cycle completion pulse
  uo[3]: "data_valid" # Data valid (for read)
  uo[4]: ""           # Unused
  uo[5]: ""           # Unused
  uo[6]: ""           # Unused
  uo[7]: ""           # Unused

  # Bidirectional pins
  uio[0]: ""          # Unused
  uio[1]: ""          # Unused
  uio[2]: ""          # Unused
  uio[3]: ""          # Unused
  uio[4]: ""          # Unused
  uio[5]: ""          # Unused
  uio[6]: ""          # Unused
  uio[7]: ""          # Unused

# Do not change!
yaml_version: 6
